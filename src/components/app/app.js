import React from 'react';
import CardForm from '../card-form';

export default class extends React.Component {
    render() {
        return (
            <>
                <CardForm />
            </>
        )
    }
}